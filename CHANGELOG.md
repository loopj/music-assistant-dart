# Changelog

## 0.2.0

- Add `[]`, `values`, and `keys` to stateful endpoints
- Add `auth` endpoint and models
- Expose models and endpoints
- Add `auth_helpers` library

## 0.1.0

- Initial development release.